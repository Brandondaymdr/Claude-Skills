<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!-- Fleet validator LaunchAgent for {{PROJECT_NAME}} -->
<!-- Installs to ~/Library/LaunchAgents/com.{{ORG_TAG}}.fleet.{{SLICE_PREFIX}}.validator.plist -->
<!-- Loads via: launchctl load ~/Library/LaunchAgents/com.{{ORG_TAG}}.fleet.{{SLICE_PREFIX}}.validator.plist -->
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.{{ORG_TAG}}.fleet.{{SLICE_PREFIX}}.validator</string>

    <key>ProgramArguments</key>
    <array>
        <string>/bin/bash</string>
        <string>{{WORKDIR}}/.fleet/bin/cron-tick.sh</string>
        <string>{{WORKDIR}}/.fleet/bin/validator.sh</string>
    </array>

    <key>WorkingDirectory</key>
    <string>{{WORKDIR}}</string>

    <key>StandardOutPath</key>
    <string>{{WORKDIR}}/.fleet/logs/validator.log</string>
    <key>StandardErrorPath</key>
    <string>{{WORKDIR}}/.fleet/logs/validator.log</string>

    <key>EnvironmentVariables</key>
    <dict>
        <key>PATH</key>
        <string>/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin</string>
        <key>HOME</key>
        <string>{{HOME}}</string>
    </dict>

    <!-- Schedule: every 30 minutes around the clock -->
    <!-- Matches CONFIG.yaml schedule.validator_interval_minutes -->
    <key>StartInterval</key>
    <integer>1800</integer>

    <key>RunAtLoad</key>
    <false/>
</dict>
</plist>
